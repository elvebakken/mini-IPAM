# Mini-IPAM Environment Configuration
# Copy this file to .env and adjust the values according to your needs

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# Secret key for session signing and token generation
# MUST be at least 32 characters long
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-to-a-long-random-string-at-least-32-characters

# ============================================================================
# DATA STORAGE
# ============================================================================

# Directory where application data is stored
# Default: /data (for Docker) or ./appdata (for local development)
DATA_DIR=/data

# ============================================================================
# SECURITY & AUTHENTICATION
# ============================================================================

# Use secure cookies (HTTPS only)
# Set to 'true' in production when using HTTPS
# Set to 'false' for development/local testing
# When set to 'true', HTTP requests will be automatically redirected to HTTPS
COOKIE_SECURE=false

# Environment setting (optional)
# Used for production mode detection
# Set to 'production' for production deployments
ENVIRONMENT=

# Session timeout in seconds
# Default: 3600 (1 hour)
# Set to 86400 for 24 hours, 1800 for 30 minutes, etc.
SESSION_TIMEOUT_SECONDS=3600

# ============================================================================
# PASSWORD POLICY
# ============================================================================

# Number of previous passwords to remember (prevents password reuse)
# Default: 5
# Set to 0 to disable password history checking
PASSWORD_HISTORY_COUNT=5

# Password expiration period in days
# Default: 0 (disabled - passwords never expire)
# Set to a positive number to enable password expiration (e.g., 90 for 90 days)
PASSWORD_EXPIRATION_DAYS=0

# ============================================================================
# MULTI-FACTOR AUTHENTICATION (MFA/2FA)
# ============================================================================

# Enable multi-factor authentication functionality
# Default: false
# When enabled, users can optionally set up MFA for their accounts
MFA_ENABLED=false

# Require MFA for all users (automatically enables MFA_ENABLED)
# Default: false
# When enabled, all users must set up MFA before they can log in
# Users cannot disable MFA when this is enabled
MFA_ENFORCE_ALL=false

# Require MFA to be enabled for users to export data
# Default: false
# When enabled, only users with MFA enabled can export data
MFA_REQUIRED_FOR_EXPORT=false

# Require MFA verification before each export operation
# Default: false
# When enabled, users must verify their MFA code before exporting data
# Provides an additional security layer for sensitive export operations
MFA_VERIFY_BEFORE_EXPORT=false

# Issuer name for TOTP (appears in authenticator apps)
# Default: Mini-IPAM
# Change this to customize the name shown in authenticator apps
MFA_ISSUER_NAME=Mini-IPAM

# ============================================================================
# AUDIT LOGGING
# ============================================================================

# Audit log retention period in days
# Default: 90
# Logs older than this will be automatically deleted
# Set to 0 to disable automatic log cleanup (not recommended)
AUDIT_LOG_RETENTION_DAYS=90
